# integration-language-python

Python integration language for siro container functions.

This is the Python counterpart to [`integration-language-r`](https://github.com/airelabsresearch/integration-language-r).
It is consumed as a git submodule in the siro monorepo under `examples/container-function-python`.
